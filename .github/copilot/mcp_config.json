{
  "$schema": "https://json.schemastore.org/mcp-config.json",
  "_comment": "MCP (Model Context Protocol) server configuration for GitHub Copilot coding agent. Add server entries to 'mcpServers' to enable tool access. See mcp_profiles/ for pre-built profiles.",
  "mcpServers": {
    "_example_filesystem": {
      "_disabled": true,
      "_comment": "Example: enable filesystem access. Remove _disabled to activate.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/workspace"],
      "env": {}
    },
    "_example_brave_search": {
      "_disabled": true,
      "_comment": "Example: enable web search. Requires BRAVE_API_KEY secret.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search"],
      "env": {
        "BRAVE_API_KEY": "${BRAVE_API_KEY}"
      }
    },
    "_example_github": {
      "_disabled": true,
      "_comment": "Example: GitHub MCP server for repo operations.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
